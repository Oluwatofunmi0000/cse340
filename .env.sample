# Local server values
PORT=5500
HOST='localhost'

# PostgreSQL Database for CSE340
# Get this from your Render.com PostgreSQL database service
# Use the "External Database URL" from the database dashboard
DATABASE_URL='postgresql://username:password@host.render.com/database_name'

# For production on Render.com, also set:
# NODE_ENV='production'